<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Task</title>
    <!-- bootstrap css -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
</head>

<body class="bg-dark text-light">
    <div class="container mt-5">
        <h2 class="text-light">Edit Task</h2>
        <form action="/insertData" method="post" class="row">
            <input type="hidden" name="editId" value="<%= task.taskId %>">
            <div class="form-group col-md-3">
                <label for="taskId" class="text-light">Task ID</label>
                <input type="text" class="form-control" id="taskId" name="taskId" placeholder="Task ID"
                    value="<%= task.taskId %>" disabled>
                <input type="hidden" name="originalTaskId" value="<%= task.taskId %>">
            </div>
            <div class="form-group col-md-6">
                <label for="taskName" class="text-light">Task Name</label>
                <input type="text" class="form-control" id="taskName" name="name" placeholder="Enter task name"
                    value="<%= task.name %>" required>
            </div>
            <div class="col-md-3">
                <label for=""></label>
                <button type="submit" class="btn btn-primary mt-2 btn-block">Update Task</button>
            </div>
        </form>
    </div>

    <!-- bootstrap js-->
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>

</html>
